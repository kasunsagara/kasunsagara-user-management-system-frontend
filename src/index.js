import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // Optional: Add if you have global CSS styles.

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

